# ワークフロールール

## コード変更のルール

1. 変更前に仕様書（design.md / requirements.md）を更新してユーザーの承諾を得る
2. 変更後に `git commit` を実行する（変更単位ごとに1コミット）
3. コミットメッセージは変更内容を簡潔に英語で記述する

## API変更のルール

- APIエンドポイントを追加・変更・削除する場合は、コード変更と同時に `docs/openapi.yaml` を更新する

## 回答言語

- 日本語で回答する
